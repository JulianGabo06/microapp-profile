# Changesets

```bash
npm run changeset          # describe el cambio y el tipo de bump
npm run changeset:status   # qué se versionaría
npm run changeset:version  # aplica el bump y escribe CHANGELOG.md
```

El paquete es privado: se versiona pero no se publica a npm ni se crean tags.
